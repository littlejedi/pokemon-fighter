import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
import {newDemo,selectionAt} from '../src/demo.js';
test('selection previews roster and locks Ash and Oak before countdown',()=>{const seen=new Set();for(let t=0;t<3.1;t+=.23){const s=selectionAt(t);seen.add(s.left);seen.add(s.right)}assert.ok(seen.size>6);assert.deepEqual(selectionAt(4),{left:0,right:14,leftLocked:true,rightLocked:true})});
test('random seeds produce different openings while keeping the bounded showcase',()=>{const a=newDemo(1,5),b=newDemo(1,40000);assert.notDeepEqual(a.events.slice(0,6),b.events.slice(0,6));assert.notDeepEqual(a.movement,b.movement);assert.deepEqual(a.events.slice(6),b.events.slice(6));assert.equal(a.events[8].at,13)});
test('archived note data remains valid and local countdown WAVs are present',()=>{const score=JSON.parse(readFileSync('assets/audio/trainer-battle.json'));assert.equal(score.channels.length,3);for(const c of score.channels){assert.equal(c.duration,96.6);assert.equal(c.loopStart,14);assert.ok(c.events.length>150);for(const [at,n,d,v] of c.events){assert.ok(at>=0&&at<c.duration);assert.ok(n>0&&n<128);assert.ok(d>0&&v>=0&&v<=1)}}for(const name of ['three','two','one','fight']){const wav=readFileSync(`assets/audio/${name}.wav`);assert.equal(wav.toString('ascii',0,4),'RIFF');assert.ok(wav.length>5000)}});
test('original battle and victory recordings are local MP3s and replace the score player',()=>{for(const name of ['trainer-battle-original','trainer-victory-original']){const data=readFileSync(`assets/audio/${name}.mp3`);assert.ok(data.length>1000000);assert.ok(data.toString('ascii',0,3)==='ID3'||data[0]===255)}const source=readFileSync('src/battle-audio.js','utf8');assert.ok(source.includes('trainer-battle-original.mp3'));assert.ok(source.includes('trainer-victory-original.mp3'));assert.ok(!source.includes('trainer-battle.json'))});
