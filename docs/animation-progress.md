# 最新：原版音乐与真正 KO 的胜利演出

原版 Game Boy 游戏战斗曲（110.04s）及胜利曲（37.032s）MP3 已下载到 assets/audio 并替换运行时合成器，浏览器成功解码、音乐通道 running；来源记录在 recordings.json。旧音谱仅留档。

演示移除 5HP 保底和 27 秒重置。在双方完成既定招式后继续通过真实攻击/命中扣血，直至大木博士 HP=0；进入 2.2 秒 KO 倒地，再进入 7 秒经典小智+皮卡丘胜利画面，显示「就决定是你了！」并播放原版胜利曲，最后重启选人。6 个随机种子验证小智存活、Oak=0、KO/胜利期间血量不重置、暂停冻结、结束再循环。33 项测试通过。实际胜利渲染器的经典贴图构图已截图验收。

---

# 最新：选人、随机开场与经典对战音乐

演示增加 select → countdown → fight 阶段，每次循环回到选人。4.8 秒展示角色卡并锁定小智与大木；3.7 秒倒数播放 Three / Two / One / Fight 本地 WAV 与提示音，期间计时、战斗均冻结。前 8.4 秒使用每轮独立种子的拳脚顺序、随机进退和跳跃；双方普通必杀及召唤仍在第 10、11.5、13、16 战斗秒固定展示。暂停、退出停止音乐，URL 入口提供手势解锁音频按钮。

音乐采用 pret/pokered 训练家对战曲音符，转换为本地 JSON，三声部 Web Audio 简化播放。四个语音 WAV 已验证非空有效采样；31 项测试通过，包含多个随机种子下的跳跃/前进/后退、按时召唤、选人锁定和倒数冻结。浏览器验证声卡 running、三声部音谱加载成功、4 个语音缓冲解码成功，选人画面可见，无控制台错误。

---

# 最新进展：三人图集与循环演示

用户认可小智分层骨骼样例，并要求只扩展大木博士和坂木；其余角色暂不处理。内置 imagegen 两个参考图请求（cell 15）均以网络错误结束，已按明确授权采用分层骨骼回退。三位各 118 帧、9 组动作已接入游戏，图集固定根节点，出拳/踢腿骨长与支撑脚验证通过，所有帧均无透明边缘裁切。

默认对手为大木博士。录屏演示 URL：`/?demo=1&clean=1`，双方按编排实际命中拳、踢、普通必杀和召唤，约 35 秒一轮；可暂停、从头重播、退出纯画面。26 项测试通过，包含完整演示循环与双边拳、踢、普通必杀、伙伴召唤分别命中的验证。浏览器确认 1280×720 纯画面布局、循环自动进入第 2 轮、Esc 退出和坂木实际贴图加载。三位图集已完成本轮视觉验收。其余训练家与宝可梦侧身素材仍不完整，不能据此宣布原五项全阵容目标完成。

---

# 当前阵容调整（2026-09-06）

用户要求继续使用内置 imagegen，并移除四天王。已将可选阵容和图鉴改为 11 人（稳定 ID 0–8、13、14），停止加载四天王的角色和伙伴贴图。原图保留作源素材存档。青绿与大木博士的选择高亮使用稳定 ID，避免删减导致错位。20 项测试通过，浏览器确认仅出现 11 张选择卡。

恢复后的第 1 轮 imagegen 请求 cell 72 已以 generations 网络错误结束，没有图片输出。尚缺 11 张训练家动作图集和 11 只伙伴侧视素材。素材提示词及验收脚本已同步为 11 人。伙伴图集新映射：第 0 张为 ID 0–4，第 1 张为 ID 5、6、7、8、13，第 2 张只含 ID 14。

以下为此前实现与阻碍记录，历史 15 人范围已被上面的 11 人要求取代。

恢复后的第 2 轮：重新核对图集清单与目录仍为空；单图 imagegen 请求 cell 2 已以同样的 generations 网络错误结束，无图片输出，无存活生成任务。继续保留 11 人范围及全部五项要求，未进行无关代码修改。

# 五项优化的验收状态

## 当前进展（2026-09-06）

1. **朝向**：站位与跨越逻辑已更新，同一物理帧内转向。原有宝可梦贴图已按原画朝向归一化。**尚未完成**：真正侧视的全套人物与宝可梦图集未生成，当前旧人物立绘仍多为正面。
2. **体型**：完成 15 只图鉴身高数据缓存与战场统一尺度。皮卡丘 0.4m、水箭龟 1.6m，屏幕身高比 1:4。大岩蛇图鉴 8.8m 为长蛇体型，以约 3m 高的盘曲姿态显示。电影特写为了构图独立缩放，不用于体型对照。
3. **召唤**：默认场上无宝可梦。3.2s 特写结束后，伙伴进入、蓄力、在 0.72s 释放一次招式，在 1.22s 开始离场、1.72s 清理；重复召唤被锁定。
4. **特写**：已实现训练家指令、伙伴蓄力、技能名称、电影黑边和推近画面。特写期间暂停物理、AI、回合倒计时；暂停/切换角色/重开取消或冻结正确。
5. **逐帧动作**：命中和收招状态机、12 帧图集渲染和帧时序已实现；**实际新序列帧素材未生成，尚未完成此项**。临时旧立绘回退不算逐帧动画。

## 当前外部阻碍

内置 image_gen 的 edits 与 generations 接口均出现 network error（向 chatgpt.com/backend-api/codex/images/... 发送请求失败）。截至本轮，参考图方式与两次单图文字方式均失败，无新 PNG 输出。工具 exec cell 46、48、51、54 均已失败结束，无存活生成任务。不要把旧图片、代码中的帧索引或绿色逻辑测试当作生成成功。

## 接续步骤

2026-09-06 第二轮接续：再次单独请求小智 12 帧透明侧身图集，exec cell 62 已以同样的 generations 网络错误结束，未返回图片。重新构建成功，20 项逻辑测试通过；素材缺失状态未改变。

2026-09-06 第三轮接续：核对 combat、partners 目录仍为空，单图重试 exec cell 67 已以同样的 generations 网络错误结束，无存活生成任务。相同外部阻碍连续三轮，任务标记 blocked，不能标记 complete。恢复条件：内置服务恢复，或用户明确授权 CLI/API 路径并在本机配置 OPENAI_API_KEY。imagegen 技能要求显式授权后才可使用 CLI 回退，目前未获授权。

- 恢复内置图像生成后，先完成小智一张图集并检查透明度、4x3 网格、朝右和 12 个真实肢体姿态，再继续其余 14 位。
- 三张 3x2 宝可梦侧视图集，每张前五格对应 5 只（最后空白），共 15 只。
- 提示词：`assets/meta/combat-prompts.json`。目标：`assets/combat/0.png` 至 `14.png`；`assets/partners/0.png` 至 `2.png`。
- 通过 `python3 scripts/inspect-atlases.py` 只读取透明边界，不编辑图片。逐张视觉核对后将 atlas 条目的 `reviewed` 设为 true。必要时重生成，不能仅改验收标记。
- `npm run audit:animation` 独立检查缺失素材与视觉验收，当前应失败。只有所有要求完成后才允许标记 goal complete。
- `tests/visual.html` 用实际渲染函数固定特写 2.45s 和体型对照，便于布局验收。

## 验证

`npm test` 的 20 项测试覆盖现有高清文件、图鉴尺度、命中时序、正面攻击判定、受击中断、跨越朝向、特写暂停、消耗一次能量、入退场、延迟弹道、回合结算。它们没有证明尚未产出的美术资产存在。
